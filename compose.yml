services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HTTP_PORT:-8080}:${HTTP_PORT:-8080}"
      - "${TCP_PORT:-3000}:${TCP_PORT:-3000}"
    environment:
      TCP_PORT: "3000"
      HTTP_PORT: "8080"